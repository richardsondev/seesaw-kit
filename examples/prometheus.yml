# Example Prometheus configuration to scrape a seesaw instance
# Adjust the username, password and host as needed
scrape_configs:
  - job_name: 'seesaw'
    metrics_path: /metrics
    basic_auth:
      username: your_user
      password: your_password
    static_configs:
      - targets: ['localhost:8001']
